spring:
  config:
    activate:
      on-profile: local
    import: optional:file:.env.local.properties[.properties]

  datasource:
    url: jdbc:mysql://localhost:3306/meetingmate?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: dbswnd2001
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: f690724f236a88d3b59462f3463e1382
            client-secret: QgIOaJBGNTCcrvonxM0vAkZuyrbLisbF

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.security.oauth2: DEBUG

app:
  frontend-redirect-url: ${APP_FRONTEND_REDIRECT_URL:http://localhost:5173/oauth/callback}

meetingmate:
  openai-api-key: ${MEETINGMATE_OPENAI_API_KEY:}

openai:
  model: ${OPENAI_MODEL:gpt-4o-mini}
